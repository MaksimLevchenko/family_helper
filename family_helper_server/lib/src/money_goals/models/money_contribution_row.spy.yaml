class: MoneyContributionRow
table: money_contribution
fields:
  goal: MoneyGoalRow?, relation
  profile: AppProfileRow?, relation
  amountCents: int
  currency: String
  note: String?
  clientOperationId: String?
  createdAt: DateTime
  revokedAt: DateTime?
indexes:
  money_contribution_goal_client_operation_idx:
    fields: goalId, clientOperationId
    unique: true
