class: PushTokenRow
table: push_token
fields:
  profileId: int
  token: String
  platform: String
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime?
  version: int
indexes:
  push_token_profile_token_idx:
    fields: profileId, token
    unique: true
