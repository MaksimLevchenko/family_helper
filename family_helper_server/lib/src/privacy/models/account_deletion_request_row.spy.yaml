class: AccountDeletionRequestRow
table: account_deletion_request
fields:
  profile: AppProfileRow?, relation
  status: String
  scheduledHardDeleteAt: DateTime
  createdAt: DateTime
  cancelledAt: DateTime?
indexes:
  account_deletion_request_profile_id_idx:
    fields: profileId
    unique: true
