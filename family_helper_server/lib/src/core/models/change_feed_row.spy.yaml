class: ChangeFeedRow
table: change_feed
fields:
  family: FamilyRow?, relation(optional)
  feature: String
  entityType: String
  entityId: int
  operation: String
  changedAt: DateTime
  tombstone: bool
  version: int
  payloadJson: String
